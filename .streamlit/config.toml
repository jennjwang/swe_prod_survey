[theme]
primaryColor = "#28a745"  # Green instead of red
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f6f6f6"
textColor = "#2c3e50"
font = "sans serif"

[server]
fileWatcherType = "poll"
maxUploadSize = 500  # 500 MB limit (use Google Form for larger files)
